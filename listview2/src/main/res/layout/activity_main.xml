<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" tools:context="com.example.listview.MainActivity">

    <EditText
        android:id="@+id/ettxt"
        android:layout_width="250dp"
        android:layout_height="43dp"
        android:layout_marginLeft="-20dp"
        android:layout_marginTop="53dp"
        android:background="@color/colorPrimary"
        android:ems="10"
        android:gravity="left|center"
        android:hint="EMOTION"
        android:inputType="textPersonName"
        android:paddingLeft="30dp"
        app:layout_constraintLeft_toLeftOf="@+id/lsview"
        app:layout_constraintTop_toBottomOf="@+id/lsview" />

    <ListView
        android:id="@+id/lsview"
        android:layout_width="368dp"
        android:layout_height="350dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="0dp"
        android:background="@color/colorPrimary"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="22dp"
        android:layout_marginTop="48dp"
        android:background="@color/colorPrimary"
        android:onClick="addEmotion"
        android:text="ADD"
        app:layout_constraintLeft_toRightOf="@+id/ettxt"
        app:layout_constraintTop_toBottomOf="@+id/lsview" />
</android.support.constraint.ConstraintLayout>
